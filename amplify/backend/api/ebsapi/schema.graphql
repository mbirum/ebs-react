type Product @model {
  id: ID!
  name: String!
  description: String
  price: String!
  quantity: Int!
  width: Int
  height: Int
  image: String
  categories: String
  createdAt: AWSDateTime
  updatedAt: AWSDateTime
}