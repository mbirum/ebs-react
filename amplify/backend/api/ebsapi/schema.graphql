type Product @model {
  id: ID!
  name: String!
  description: String
  slug: String!
  price: String!
  quantity: Int!
  width: Int
  height: Int
  image: String
  additionalImages: String
  categories: String
  createdAt: AWSDateTime
  updatedAt: AWSDateTime
}